# Coerce sparse_numeric to numeric via as.numeric()

Coerce sparse_numeric to numeric via as.numeric()

## Usage

``` r
# S4 method for class 'sparse_numeric'
as.numeric(x)
```

## Arguments

- x:

  A `sparse_numeric` object.

## Value

A numeric vector of length `x@length`.
